# Core
# Django 6.0 requires Python >= 3.12. Pin to the 6.0.x series (>=6.0.3 picks
# up the CVE-2026-1285 / CVE-2026-1287 / CVE-2026-1312 security fixes).
Django
gunicorn
whitenoise               # 6.9+ adds Django 5.2/6.0 support; uses the STORAGES setting
python-decouple

# Database
dj-database-url          # required by settings.py DATABASE_URL path

# MySQL driver (Django 6 requires mysqlclient>=2.2.1; MySQL 8.0.11+ / MariaDB 10.6+)
mysqlclient>=2.2.1,<3

# Media
Pillow

# Admin theme
django-unfold>=0.60,<1            # check PyPI for the latest 0.9x release for best Django 6 support

# HTML sanitisation
bleach>=6.1,<7

# Auth / security
django-axes             # 8.x adds Django 6.0 support
# django-csp removed: Django 6.0 ships its own CSP support via
# django.middleware.csp.ContentSecurityPolicyMiddleware + the SECURE_CSP
# setting (see myproject/settings.py). The 'csp' app is no longer required.

# File-type detection
python-magic
# python-magic-bin is Windows-only — uncomment only on Windows:
python-magic-bin

# Translations (remove if not actively using)
django-rosetta>=0.10,<1          # NOT in INSTALLED_APPS — remove or enable
django-modeltranslation>=0.18,<1  # NOT in INSTALLED_APPS — remove or enable

# 2FA (remove if not actively using)
# django-two-factor-auth>=1.16,<2  # NOT in INSTALLED_APPS — remove or enable
pyotp
qrcode[pil]

# Markdown rendering (body_md_file feature)
markdown>=3.7
pymdown-extensions>=10.0    # admonitions, arithmatex (LaTeX), syntax highlighting

# Dev only — do not install in production
# django-extensions>=3.2,<4
